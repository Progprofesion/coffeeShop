// import { useSelector } from 'react-redux';
// import { RootState } from 'src/store/index';
// import { useLocalStorage } from '../hooks/useLocalStorage';

// export const personalBasket = () => {

//     const [localEmail, setLocalEmail] = useLocalStorage('persStateArr', 0);
//     const [localToken, setLocalToken] = useLocalStorage('accessToken', 0);
//     const [localId, setLocalid] = useLocalStorage('id', 0);

//     const userId = useSelector((state: RootState) => state.user.id);

//     // if (localStorage.getItem('userId')) {
//     //     localStorage.setItem('persStateArr', userId);
//     // }

// }
